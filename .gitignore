# west-managed dependencies and workspace metadata
/deps/
/.west/

# python virtualenv
/.venv/

# build output
/build/
/app/build/

# local WiFi credentials — never committed
/app/wifi_credentials.conf

# compiled test modules and the LLVM pass plugin
/tools/*.wasm
/tools/wasp-remote-pass/*.so

__pycache__/
*.pyc
