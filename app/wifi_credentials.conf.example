# Copy to app/wifi_credentials.conf (gitignored) and fill in. The build
# merges that file automatically when it exists; it must never be
# committed. Empty PSK means an open network.
CONFIG_WASP_WIFI_SSID="your-network-name"
CONFIG_WASP_WIFI_PSK="your-password"
